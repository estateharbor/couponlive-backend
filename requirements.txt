# --- Web / API ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- Database ---
SQLAlchemy>=2.0.30
alembic>=1.13.0
psycopg[binary]>=3.2.0          # Postgres driver (works with Supabase/Neon/local)

# --- Queue / scheduling ---
celery>=5.4.0
redis>=5.0.0

# --- Scraping (Phase 2+) ---
requests>=2.32.0
beautifulsoup4>=4.12.0
lxml>=5.2.0
rapidfuzz>=3.9.0                # fuzzy merchant-name matching for dedup

# --- Headless browser (Phase 3/4, only where required) ---
playwright>=1.44.0

# --- Ops / logging ---
structlog>=24.1.0
tenacity>=8.3.0                 # retry/backoff utilities

# --- Dev / test ---
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0                   # FastAPI TestClient dependency
