[pytest]
markers =
    live: hits real external sites (scraper selector health checks). Opt in with `-m live`.
    browser: runs a real headless browser against a local fixture. Opt in with `-m browser`.
filterwarnings =
    ignore::DeprecationWarning
